# FastAPI target used by the Vite development proxy.
ALPHABRAIN_API_URL=http://127.0.0.1:8000

# Optional public API base. Keep /api/v1 when the backend serves the built UI.
VITE_API_BASE_URL=/api/v1
